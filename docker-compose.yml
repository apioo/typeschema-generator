version: '3'
services:
  generator:
    build: .
    environment:
      FORMAT: "${FORMAT}"
      NAMESPACE: "${NAMESPACE}"
      SCHEMA_FILE: "${SCHEMA_FILE}"
    volumes:
      - ./output:/usr/src/typeschema/output
